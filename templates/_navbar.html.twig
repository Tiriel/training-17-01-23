{% macro classes(route, currentRoute) %}{{ 'nav-link'}}{{ route != currentRoute ? '': ' active' }}{% endmacro %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_main_index') }}">Sensio TV+</a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="{{ _self.classes('app_main_index', currentRoute) }}" href="{{ path('app_main_index') }}">Home</a>
            </li>
            <li class="nav-item">
                <a class="{{ _self.classes('app_book_index', currentRoute) }}" href="{{ path('app_book_index') }}">Book</a>
            </li>
            <li class="nav-item">
                <a class="{{ _self.classes('app_movie_index', currentRoute) }}" href="{{ path('app_movie_index') }}">Movie</a>
            </li>
            <li class="nav-item">
                <a class="{{ _self.classes('app_main_contact', currentRoute) }}" href="{{ path('app_main_contact') }}">Contact</a>
            </li>
        </ul>
    </div>
</nav>
